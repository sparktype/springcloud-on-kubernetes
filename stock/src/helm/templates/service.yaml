apiVersion: v1
kind: Service
metadata:
  name: {{ include "stock.fullname" . }}
  labels:
    app: {{ template "stock.name" . }}
    chart: {{ template "stock.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "stock.name" . }}
    release: {{ .Release.Name }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ template "stock.fullname" . }}-preview
  labels:
    app: {{ template "stock.name" . }}
    chart: {{ template "stock.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "stock.name" . }}
    release: {{ .Release.Name }}