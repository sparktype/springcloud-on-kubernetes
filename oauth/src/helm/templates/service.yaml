apiVersion: v1
kind: Service
metadata:
  name: {{ include "oauth.fullname" . }}
  labels:
    app: {{ template "oauth.name" . }}
    chart: {{ template "oauth.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "oauth.name" . }}
    release: {{ .Release.Name }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ template "oauth.fullname" . }}-preview
  labels:
    app: {{ template "oauth.name" . }}
    chart: {{ template "oauth.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "oauth.name" . }}
    release: {{ .Release.Name }}