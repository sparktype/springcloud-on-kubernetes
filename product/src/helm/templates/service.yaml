apiVersion: v1
kind: Service
metadata:
  name: {{ include "product.fullname" . }}
  labels:
    app: {{ template "product.name" . }}
    chart: {{ template "product.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "product.name" . }}
    release: {{ .Release.Name }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ template "product.fullname" . }}-preview
  labels:
    app: {{ template "product.name" . }}
    chart: {{ template "product.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "product.name" . }}
    release: {{ .Release.Name }}