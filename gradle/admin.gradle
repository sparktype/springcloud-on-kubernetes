configure(subprojects.findAll { it.name.startsWith("springboot-") }) {

  springBoot {
    buildInfo()
  }

  dependencies {
    implementation("org.springframework.cloud:spring-cloud-starter-netflix-eureka-client")
  }
}
