configure(subprojects.findAll { it.name == "springboot-admin" }) {

  springBoot {
    buildInfo()
  }

  dependencies {
    implementation("org.springframework.cloud:spring-cloud-starter-netflix-eureka-client")
  }
}
