apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-user
  namespace: springcloud
spec:
  replicas: 1
  selector:
    matchLabels:
      api: api-user
  template:
    metadata:
      labels:
        api: api-user
    spec:
      containers:
        - name: api
          image: cloudcamp/api-user:latest
          ports:
            - containerPort: 8080
          env:
            - name: spring.profiles.active
              valueFrom:
                configMapKeyRef:
                  name: springcloud
                  key: spring.profile
            - name: spring.cloud.config.uri
              valueFrom:
                configMapKeyRef:
                  name: springcloud
                  key: springcloud.config.uri
            - name: eureka.client.serviceUrl.defaultZone
              valueFrom:
                configMapKeyRef:
                  name: springcloud
                  key: springcloud.eureka.uri
            - name: zipkin.

              zipkin:
                uri:
---
apiVersion: v1
kind: Service
metadata:
  name: api-user
  namespace: springcloud
spec:
  selector:
    api: api-user
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
